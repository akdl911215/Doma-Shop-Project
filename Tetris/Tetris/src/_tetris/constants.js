// 게임 설정과 규칙을 정의
const COLS = 10; // 열
const ROWS = 20; // 행
const BLOCK_SIZE = 30;

const KEY = {
    LEFT: 37,
    RIGHT: 39,
    DOWN: 40,
};
Object.freeze(KEY);

// gogo
export { COLS, ROWS, BLOCK_SIZE, KEY };
